<Window x:Class="DesktopManager.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopManager"
        mc:Ignorable="d"
        Title="当时我就念了两句诗" Height="350" Width="525">
    <Grid>
        <TextBlock Height="40" TextAlignment="Center" IsEnabled="False" Style="{DynamicResource MaterialDesignTitleTextBlock}" Name="Title" Margin="0,0,0,269" VerticalAlignment="Bottom" HorizontalAlignment="Left" Width="517"></TextBlock>
        <TextBlock Name="PoetryContent" TextAlignment="Center" IsEnabled="False" Style="{DynamicResource MaterialDesignSubheadingTextBlock}" Margin="0,66,0,0"></TextBlock>
        <Button Name="RandomPoetry" Style="{DynamicResource MaterialDesignFlatButton}" Click="RandomPoetry_Click">点一下念首诗</Button>
    </Grid>
</Window>